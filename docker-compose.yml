version: '3.8'

services:
  machu-app:
    build: .
    container_name: machu-import-service
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: always
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
